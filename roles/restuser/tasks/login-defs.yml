- lineinfile:
    dest: /etc/login.defs
    regexp: '^(UMASK\s+).+$'
    line: '\g<1>022'
    backrefs: yes

- lineinfile:
    dest: /etc/login.defs
    regexp: '^(HOME_MODE\s+).+$'
    line: '\g<1>0755'
    backrefs: yes
