---
# Inventory file hostname of the NFS server (note this should be an Ansible
# alias, not the real hostname)
nfsserver: ''
# Options to mount NFS with
nfsoptions: rw,_netdev,auto
# Where the exchange directory should be mounted on the client
exchange: ''
